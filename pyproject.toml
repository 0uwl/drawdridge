[project]
name = "drawbridge"
version = "0.0.1"
description = "Hardened classic ZTP system for Cisco IOS XE devices"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
